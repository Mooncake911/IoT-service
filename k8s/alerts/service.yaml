apiVersion: v1
kind: Service
metadata:
  name: iot-alerts
  namespace: iot
  labels:
    app: iot-alerts
spec:
  selector:
    app: iot-alerts
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  type: ClusterIP
