apiVersion: v1
kind: Service
metadata:
  name: iot-controller
  namespace: iot
  labels:
    app: iot-controller
spec:
  selector:
    app: iot-controller
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  type: ClusterIP
