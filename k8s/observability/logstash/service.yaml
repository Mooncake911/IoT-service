apiVersion: v1
kind: Service
metadata:
  name: logstash
  namespace: iot
  labels:
    app: logstash
spec:
  selector:
    app: logstash
  ports:
    - port: 5044
      targetPort: 5044
      name: beats
  type: ClusterIP