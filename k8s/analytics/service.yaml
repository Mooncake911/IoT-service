apiVersion: v1
kind: Service
metadata:
  name: iot-analytics
  namespace: iot
  labels:
    app: iot-analytics
spec:
  selector:
    app: iot-analytics
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  type: ClusterIP
